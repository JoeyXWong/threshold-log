# Copy to .env for local builds. In GitHub Actions these come from repo secrets.
# Both values are from Supabase → Project Settings → API. The anon key is
# designed to ship in client code; row-level security protects the data.
SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
SUPABASE_ANON_KEY=eyJ...
